nerdfont: true
agent: claude

layout:
  panes:
    - command: bash       # Left pane: boots into a normal shell
      focus: true
    - split: vertical     # Splits the window vertically
      command: <agent>    # Right pane: boots Claude here
      focus: false         # Automatically puts your keyboard focus on Claude
