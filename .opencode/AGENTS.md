# General Notes

- Never put trailing spaces in code or shell scripts

# Here's a list of things in ~/.config that are symlinked into this project (dotfiles). So you don't need to ask permission to access them in ~/.config, just access them here instead:
- bat
- gh-dash/config.yml
- iterm2/AppSupport
- nushell
- sketchybar
- starship.toml
- television
- tmuxinator
- init.el
